# sil-ts — TypeScript IEC 61508-6 Engine

**Status:** Included in the private PRISM repository — will be extracted and published here.

This package will contain:
- PFDavg and PFH for all standard IEC 61508-6 architectures
- Same formulas as `sil-py`, optimized for real-time browser computation
- TypeScript types for all parameters and results
- npm package `@sil-engine/ts`

## Placeholder

The TypeScript engine is being prepared for open-source release.  
Watch this repo for updates or check [CHANGELOG.md](../../CHANGELOG.md).

If you need the TypeScript engine now, the Python engine produces identical results
for all IEC 61508-6 Annex B formulas and can be called via the REST API.
